FlowControlLoopsCollections.votingEligibility();

        Integer age = 20; // Replace with the age you want to test
        Boolean isEligible = VotingEligibilityChecker.votingEligibility(testAge);
        System.debug('Is the person eligible to vote? ' + isEligible);
 
